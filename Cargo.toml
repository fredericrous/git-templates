[workspace]
resolver = "2"
members = ["crates/githooks", "crates/githooks-runtime"]

# Shared release posture. The hook binary runs on every commit, so size and
# start-up cost are features.
[profile.release]
strip = true
opt-level = "s"
lto = true
panic = "abort"
